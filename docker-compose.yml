services:
  php:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: make test
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1

  php-interactive:
    build: .
    working_dir: /app
    stdin_open: true
    tty: true
    volumes:
      - .:/app
    command: sh
